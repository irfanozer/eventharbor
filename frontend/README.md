# EventHarbor dashboard

The React and TypeScript dashboard is the next product milestone. It will expose
the Control Room, event timeline, endpoints, dead letters, and Receiver Lab
without hiding the system's state transitions behind generic charts.
